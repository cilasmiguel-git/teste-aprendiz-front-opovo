/**
 * Arquivo principal de scripts da Landing Page
 * Desenvolvido como parte do teste para o Grupo O POVO
 */

document.addEventListener("DOMContentLoaded", () => {
    // Nota: Inicializações do jQuery e OwlCarousel foram enviadas dinamicamente
    // direto para dentro do lifecycle (connectedCallback) dos Web Components 
    // específicos que usam o Carrossel (Elenco.js e Resenhas.js).

    console.log("O POVO: Interface carregada via Vanilla Web Components e CSS Modular com Sucesso!");
});
